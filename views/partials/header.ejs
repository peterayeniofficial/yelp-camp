<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>YelpCamp</title>
    <link rel="stylesheet" href="/stylesheets/page.css" type="text/css">
    <link rel="stylesheet" href="/stylesheets/page.css.map" type="text/css">
    <link rel="stylesheet" href="/stylesheets/style.css" type="text/css">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-stick-dark" data-navbar="sticky">
            <div class="container">
      
              <div class="navbar-left">
                <button class="navbar-toggler" type="button">&#9776;</button>
                <a class="navbar-brand " href="/">
                  <img class="logo-dark" src="/img/YelpCamp.png" alt="logo">

                </a>
              </div>
      
              <section class="navbar-mobile">
                <span class="navbar-divider d-mobile-none"></span>
      
                <nav class="nav nav-navbar mr-auto">
                    <a class="nav-link" href="/campgrounds">Camps</span></a>
                    <a class="nav-link" href="/campgrounds/new">Add new camp ground</span></a>
                </nav>
              
              <div>
                <% if(!currentUser){ %>
                  <a class="btn btn-sm btn-success" href="/login">Login</a>

                <% } else { %>
                  <a class="btn btn-sm btn-light ml-lg-5 mr-2" href="#">Hi! <%= currentUser.username %></a>
                  <a class="btn btn-sm btn-success" href="/logout">Logout</a>

                <% } %>
              </div>
            </section>
            </div>
          </nav><!-- /.navbar -->

         
            <div class=" container mt-10">
              <% if (error && error.length > 0) { %>
                <div class="alert alert-danger">
                  <%= error %>
                </div>
              <% } %>

              <% if(success && success.length > 0) { %>
                <div class="alert alert-success">
                  <%= success %>
                </div>
              <% } %>
            </div>
         
        
    
